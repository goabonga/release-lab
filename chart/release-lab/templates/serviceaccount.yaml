{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "release-lab.fullname" . }}
  labels:
    {{- include "release-lab.labels" . | nindent 4 }}
{{- end }}
